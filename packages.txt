packages.txt: System-level dependencies required for heavy Python libraries

(like trimesh and PyTorch's underlying dependencies) to install correctly.

OpenGL library (often required by trimesh/rendering tools)

libgl1-mesa-glx

Standard X Windows System libraries (for graphics/window management)

libsm6
libxext6
