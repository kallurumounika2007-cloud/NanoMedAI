packages.txt: System-level dependencies required for heavy Python libraries

(like trimesh, pythreejs, and PyTorch's underlying dependencies) to install correctly.

OpenGL library (essential for many 3D/graphics/visualization tools)

libgl1-mesa-glx

Standard X Windows System libraries (often needed by visualization backend compilation)

libsm6
libxext6
